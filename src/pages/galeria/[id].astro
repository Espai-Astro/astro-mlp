---
import Layout from '../../layouts/Layout.astro';
import TargetaArt from '../../components/TargetaArt.astro';


const { id: paramId } = Astro.params;
const id = Number(paramId?.substring(3)) - 1;
console.log('id -->', id);
---

<Layout titol={`Targeta Art${id + 1}`}>
	<TargetaArt id={id} />
</Layout>
